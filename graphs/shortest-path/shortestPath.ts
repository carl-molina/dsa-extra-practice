import { GNodeStr } from "../common/graph";
import { Queue } from "../common/queue";

/**shortestPath
 *
 * Finds the shortest path from start node to end node and returns
 * path-values or null if no path.
 *
 * start/end: { value: "", adjacent: [ {}, {} ] }
 */

function shortestPath(start: GNodeStr, end: GNodeStr): GNodeStr[] | null {
  return null;
}


export { shortestPath };