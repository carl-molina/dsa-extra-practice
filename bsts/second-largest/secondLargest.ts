import { BNodeNum } from "../common/bst";

/** findSecondLargest(): Find the second-largest value in the BST, if it exists.
 *
 * Otherwise, returns null.
 **/

function findSecondLargest(node: BNodeNum | null): number | null {
  return null;
}

export { findSecondLargest };