import { BNodeNum} from "../common/bst";

/** Is BST balanced?
 *
 * A balanced BST has it's shallowest node only, at most, one level up
 * from its deepest node.
 */

function isBalanced(root: BNodeNum | null): boolean {
  return false;
}

export { isBalanced  };