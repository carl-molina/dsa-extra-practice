import { BinTreeNum } from "../common/bintree";

/** Maximum sum of a path through the tree.
 *
 * The path doesn't need to start at the root, but this won't visit the
 * same node more than once.
 *
 *          5       the highest-sum for this would be 11
 *        /   \          [2, -1, 5, 3, -4, 6]
 *      -1     3
 *     / \    /  \
 *    1   2  -3  -4
 *               /
 *              6
 **/

function maxPath(tree: BinTreeNum) {
  return 0;
}

export { maxPath };