function pivot(nums: number[], start = 0, end = nums.length - 1): number {
  return 42;
}

function quickSort(nums: number[], left = 0, right = nums.length - 1) {
  return nums;
}

export { quickSort, pivot };