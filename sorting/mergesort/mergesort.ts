/** Merge two already-sorted arrays and return merge. */

function mergesort<T>(arr1: T[], arr2: T[]): T[] {
  return [];
}

/** Sort array and return sorted version. */

function mergeSort<T>(arr: T[]): T[] {
  return [];
}

export { mergesort, mergeSort };
