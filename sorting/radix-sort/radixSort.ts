/** Extract i'th digit from num: (12345, 1) => 4 */

function getDigit(num: number, i: number): number {
  return 0;
}


/** Get count of digits in num: 12345 => 5. */

function getDigitCount(num: number): number {
  return 0;
}


/** Return highest count of digits from nums in nums. */

function getHighDigitCount(nums: number[]): number {
 return 0;
}


/** Sort (in-place) and return sorted numbers. */

function radixSort(nums: number[]): number[] {
  return nums;
}


export { radixSort, getDigitCount, getDigit, getHighDigitCount };